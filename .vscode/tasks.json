{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pipInstall cognitive",
            "type": "shell",
            "command": "${config:python.pythonPath} -m pip install -r ${workspaceFolder}/data/preparation/cognitive/requirements.txt",
            "problemMatcher": []
        }
    ]
}